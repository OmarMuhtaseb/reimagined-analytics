export enum AnalyticsCategory {
    category = 'category',
    product = 'product',
    contact = 'contact',
}

export enum AnalyticsAction {
    select = 'select',
    deselect = 'deselect',
    productClickDetails = 'click_details',
    productViewDetails = 'view_details',
    imageView = 'image_view',
    messageSubmitted = 'message_submit',
}
